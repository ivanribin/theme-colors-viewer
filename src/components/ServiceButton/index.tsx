import type { ReactElement } from "react"
import "./style.css"

export const ServiceButton = ():ReactElement => {
    return (
        <button className="service-button">
            SEE ALL SERVICES
        </button>
    )
}