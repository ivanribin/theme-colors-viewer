import { type ReactElement } from "react";
import "./style.css";

const ColorsSettingsPage = (): ReactElement => {

    return(
        <div className="colors-settings-page">Colors Settings Page</div>
    )
};

export default ColorsSettingsPage;